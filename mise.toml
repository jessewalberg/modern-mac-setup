[tools]
node = "24.18.0"
ruby = "4.0.6"
shellcheck = "0.11.0"
shfmt = "3.13.1"

[tasks.lint]
description = "Run the complete repository validation suite"
run = "make lint"

[tasks.test]
description = "Run mocked bootstrap, audit, and idempotence tests"
run = "make test"
